p = $pat_gorila+0;
x = rnd( 745 * 0.5, $screenWidth - 745 );
y = rnd( 1146 * 0.5, $screenHeight - 1146 );
angle = 0;
scaleX = 1;

while( true ){
    // scaleX = cos( angle ) * 10;
    if( angle > 360 ) die();
    
    update();
}